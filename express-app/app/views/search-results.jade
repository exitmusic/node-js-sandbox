extends templates/standard

block append css
  link(rel='stylesheet', href='/css/search-results.css')

block content
  h1 Search Results
  #results-list
    #filter-search
      label(for="filter") Filter Results:
        each term, iter in searchTerms
          span  #{term}
      input(id="filter", class="search", placeholder="Type to filter results")
    ul#results.list
      if (results.length === 0)
        li.result.clearfix
          p.filename No files found
      else
        each result, iter in results
          li.result.clearfix(class="#{iter % 2 === 0 ? '' : 'odd'}")
            h2.result-num ##{iter + 1}
            .audio-file
              audio(src="/media/#{result.filename}", controls)
                p Your browser doesn't support HTML5 audio
            p.filename #{result.path}
            p
              a(href="/media/#{result.filename}") #{result.filename}


block append script
  script(src="/js/lib/list.min.js")
  script(src="/js/search-results.js")
